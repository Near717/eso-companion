--[[ -- Define configuration variables
local config = {
    server = "NA Megaserver", -- change this to desired server
    userId = "@notnear" -- change this to the account to be tracked
}

-- Return the configuration table
return config
 ]]